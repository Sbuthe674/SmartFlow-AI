# 🚀 Быстрый Старт: Система Логина и Регистрации

## ⚡ Что было реализовано

### 1️⃣ Frontend
- ✅ Модальное окно логина (email + пароль)
- ✅ Модальное окно регистрации (компания + email + пароль)
- ✅ Кнопка администратора в хедере справа
- ✅ Сохранение сессии в localStorage
- ✅ Показ имени пользователя после логина

### 2️⃣ Backend
- ✅ API для регистрации пользователя
- ✅ API для логина с JWT токеном
- ✅ API для регистрации администратора
- ✅ Хеширование паролей (bcrypt)
- ✅ Проверка подлинности токена

### 3️⃣ База данных
- ✅ Таблица `users` с полями: username, email, hashed_password, company_name, is_admin, is_active

---

## 📦 Установка

### Шаг 1: Установите зависимости
```bash
cd /Users/smvpt/Documents/GitHub/SmartFlow-AI
pip install -r requirements.txt
```

**Необходимые пакеты:**
- fastapi, uvicorn
- sqlalchemy, pydantic
- passlib[bcrypt] (для хеширования паролей)
- python-jose (для JWT)
- pydantic[email] (для валидации email)

### Шаг 2: Подготовьте переменные окружения
```bash
# Скопируйте шаблон
cp env/.env.example env/.env

# Отредактируйте env/.env и добавьте:
SECRET_KEY=your-super-secret-key-min-32-symbols
```

### Шаг 3: Инициализируйте БД (автоматически)
База данных автоматически создается при первом запуске backend

---

## 🏃 Запуск

### Запуск Backend
```bash
cd /Users/smvpt/Documents/GitHub/SmartFlow-AI/backend
python main.py
```

Должно появиться:
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

### Запуск Frontend
Откройте в браузере:
```
file:///Users/smvpt/Documents/GitHub/SmartFlow-AI/frontend/index.html
```

Или используйте Live Server:
```bash
# Если установлен VSCode Live Server
# Кликните правой кнопкой на index.html -> Open with Live Server
```

---

## 🧪 Тестирование

### Тест 1: Регистрация через UI
1. Откройте фронтенд
2. Нажмите кнопку "Войти" в хедере
3. Перейдите на вкладку "Зарегистрироваться"
4. Заполните форму:
   - Имя компании: `My Company`
   - Email: `myemail@test.com`
   - Пароль: `TestPass123`
5. Нажмите "Зарегистрироваться"

**Ожидаемый результат:**
- ✅ Кнопка "Войти" изменится на "👤 My Company"
- ✅ Модальное окно закроется
- ✅ Токен сохранится в localStorage

### Тест 2: Логин через UI
1. Откройте фронтенд (или обновите страницу)
2. Нажмите кнопку "Войти"
3. Введите:
   - Email: `myemail@test.com`
   - Пароль: `TestPass123`
4. Нажмите "Войти"

**Ожидаемый результат:**
- ✅ Успешный логин
- ✅ Сохранение токена в localStorage

### Тест 3: Регистрация администратора
1. Нажмите кнопку "👤 Admin" в верхнем правом углу
2. Заполните форму:
   - Имя: `admin_user`
   - Email: `admin@test.com`
   - Пароль: `AdminPass123`
3. Нажмите "Зарегистрировать"

**Ожидаемый результат:**
- ✅ Администратор успешно зарегистрирован
- ✅ Флаг `is_admin = True` в БД

### Тест 4: Автоматизированное тестирование
```bash
cd backend
python test_auth.py
```

Скрипт протестирует:
- ✅ Регистрацию пользователя
- ✅ Логин и получение токена
- ✅ Получение профиля пользователя
- ✅ Регистрацию администратора
- ✅ Отклонение дубликатных email

---

## 📡 API Endpoints

### Регистрация пользователя
```http
POST /api/auth/register
Content-Type: application/json

{
  "username": "company_name",
  "email": "user@example.com",
  "password": "SecurePass123",
  "company_name": "My Company"
}
```

**Ответ (успешно):**
```json
{
  "success": true,
  "message": "User registered successfully",
  "data": {
    "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "token_type": "bearer",
    "user": {
      "id": 1,
      "username": "company_name",
      "email": "user@example.com",
      "company_name": "My Company",
      "is_admin": false,
      "is_active": true,
      "created_at": "2025-12-06T10:00:00"
    }
  }
}
```

### Логин пользователя
```http
POST /api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "SecurePass123"
}
```

### Регистрация администратора
```http
POST /api/auth/admin/register
Content-Type: application/json

{
  "username": "admin_name",
  "email": "admin@example.com",
  "password": "AdminPass123"
}
```

### Получить профиль текущего пользователя
```http
GET /api/auth/me
Authorization: Bearer {access_token}
```

### Логаут
```http
POST /api/auth/logout
```

---

## 🔐 Безопасность

### ✅ Реализовано
- Пароли хешируются с bcrypt (не хранятся в открытом виде)
- JWT токены для аутентификации (30 минут)
- CORS настроена для фронтенда
- Валидация email
- Проверка дубликатных email/username

### 🔄 Следующие шаги (TODO)
- [ ] Refresh токены для продления сессии
- [ ] Email подтверждение при регистрации
- [ ] Двухфакторная аутентификация (2FA)
- [ ] Ограничение по IP адресу
- [ ] Логирование попыток входа
- [ ] Восстановление пароля
- [ ] Защита от brute-force атак

---

## 🗂️ Файлы проекта

### Frontend
- `frontend/index.html` - Модали логина и регистрации
- `frontend/script.js` - Обработчики форм и токенов
- `frontend/style.css` - Стили модалей

### Backend
- `backend/models.py` - SQLAlchemy модель User
- `backend/router_auth.py` - API маршруты аутентификации
- `backend/main.py` - Подключение router_auth
- `backend/config.py` - SECRET_KEY и параметры JWT
- `backend/test_auth.py` - Автоматизированные тесты

### Конфигурация
- `env/.env.example` - Шаблон переменных окружения
- `requirements.txt` - Зависимости Python

---

## 🐛 Возможные ошибки и решения

### Ошибка: `ModuleNotFoundError: No module named 'fastapi'`
**Решение:**
```bash
pip install fastapi uvicorn sqlalchemy pydantic passlib python-jose
```

### Ошибка: `Connection refused` при обращении к API
**Решение:**
1. Убедитесь, что backend запущен на `http://localhost:8000`
2. Проверьте, что CORS включен в `main.py`
3. Проверьте порт 8000 не занят: `lsof -i :8000`

### Ошибка: Пароли не совпадают при регистрации
**Решение:**
- Убедитесь, что пароли точно совпадают (регистр важен)
- Пароль должен быть минимум 6 символов

### Ошибка: Email уже зарегистрирован
**Решение:**
- Используйте другой email
- Или удалите запись из БД: `sqlite3 backend/helpdesk.db "DELETE FROM users WHERE email='email@test.com';"`

---

## 📝 Дополнительные команды

### Просмотр БД
```bash
# Установите sqlite3 если не установлен
sqlite3 backend/helpdesk.db

# Просмотрите пользователей
sqlite> SELECT * FROM users;

# Выход
sqlite> .exit
```

### Сброс БД
```bash
# Удалите файл БД (все данные будут потеряны)
rm backend/helpdesk.db

# БД будет пересоздана при следующем запуске backend
```

### Просмотр логов
```bash
# Backend выводит логи в консоль, используйте:
python main.py 2>&1 | tee backend.log
```

---

## ✨ Что дальше?

1. **Admin Dashboard** - создайте страницу для управления пользователями
2. **Email Verification** - отправляйте email при регистрации
3. **Password Reset** - функция восстановления пароля
4. **Role-Based Access** - разные права для admin/user/operator
5. **Activity Logging** - логирование всех действий пользователей
6. **2FA** - двухфакторная аутентификация

---

## 📚 Полезные ссылки

- FastAPI Docs: https://fastapi.tiangolo.com/
- SQLAlchemy Docs: https://docs.sqlalchemy.org/
- JWT.io: https://jwt.io/
- Passlib Docs: https://passlib.readthedocs.io/

---

**Готово к использованию! 🎉**
