# Telegram Bot для AI HelpDesk OneWindow

Telegram-бот использует **Long Polling** для получения сообщений от пользователей, отправляет их в backend API и возвращает результаты.

## 📋 Требования

- Python 3.8+
- Пакеты: `requests`, `python-dotenv`
- Backend AI HelpDesk запущен на `http://localhost:8000`

## 🚀 Запуск

### 1. Установка зависимостей

```bash
cd backend
pip install requests python-dotenv
```

### 2. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env` и добавьте ваш Telegram токен:

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_TOKEN=8283540866:AAES_K_VXOOEWh7vOK6JpTD4adnbs6wyMVM
BACKEND_URL=http://localhost:8000
```

**Как получить TELEGRAM_TOKEN:**

1. Откройте Telegram и найдите бота `@BotFather`
2. Напишите `/newbot`
3. Следуйте инструкциям
4. Скопируйте выданный токен

### 3. Запуск бота

**Убедитесь, что backend запущен:**

```bash
# В одном терминале - запуск backend
cd backend
python -m uvicorn main:app --reload
```

**В другом терминале - запуск бота:**

```bash
# Windows
cd backend
python telegram_bot.py

# Linux/Mac
cd backend
python telegram_bot.py
```

Вы должны увидеть:

```
2024-12-05 10:30:45,123 - INFO - ✅ Конфиг загружен. Backend: http://localhost:8000
2024-12-05 10:30:45,124 - INFO - 🤖 Telegram bot started...
2024-12-05 10:30:45,125 - INFO - 📡 Подключение к Telegram API через Long Polling...
```

## 🔄 Как работает бот

### Архитектура взаимодействия

```
Пользователь (Telegram)
         ↓
    [Telegram API]
         ↓
 telegram_bot.py (Long Polling getUpdates)
         ↓
   process_user_message()
         ↓
[Backend API: POST /api/ingest]
         ↓
Backend AI HelpDesk Core (классификация, FAQ)
         ↓
Ответ: auto-resolve или create ticket
         ↓
   send_message()
         ↓
    [Telegram API]
         ↓
Пользователь получает ответ
```

### Вариант 1: Автоматическое решение

Если обращение совпадает с FAQ (сходство > 75%):

```
Backend ответит:
{
  "status": "closed_auto",
  "answer": "Чтобы подключиться к VPN...",
  "similarity_score": 0.92
}

Пользователь получит:
🤖 Автоматическое решение:
Чтобы подключиться к VPN...
(Уверенность: 92%)
```

### Вариант 2: Создание тикета

Если обращение требует ручной обработки:

```
Backend ответит:
{
  "status": "new",
  "ticket_id": 42,
  "category": "VPN",
  "priority": "high",
  "department": "IT_Infrastructure"
}

Пользователь получит:
📝 Ваш запрос зарегистрирован!

ID тикета: #42
Категория: VPN
Приоритет: high
Отдел: IT_Infrastructure

Наш специалист скоро ответит!
```

## 🌍 Поддержка языков

Бот автоматически определяет язык текста (русский или казахский) и передаёт его в backend:

```python
language = detect_language(text)  # "ru" или "kz"

payload = {
    "text": text,
    "language": language
}
```

Backend обработает текст согласно определённому языку.

## 📡 Long Polling механизм

Бот использует `getUpdates` с `timeout=100`:

```python
def get_updates(offset: int = 0):
    url = f"{TELEGRAM_API_URL}/getUpdates"
    params = {
        "offset": offset,
        "timeout": 100,  # Сервер ждёт новые сообщения 100 сек
        "allowed_updates": ["message"]
    }
    response = requests.get(url, params=params, timeout=110)
    return response.json()
```

**Преимущества Long Polling:**
- ✅ Нет необходимости в вебхуке / HTTPS
- ✅ Работает за NAT / firewall
- ✅ Проще развернуть локально
- ✅ Подходит для development и MVP

## ⚙️ Функции бота

### `send_message(chat_id, text)`

Отправляет сообщение пользователю:

```python
send_message(123456, "Привет! 👋")
```

**Параметры:**
- `chat_id` (int): ID чата пользователя
- `text` (str): Текст сообщения (поддерживает HTML форматирование)

**Возвращает:** `True` если успешно, `False` если ошибка

### `process_user_message(text, chat_id)`

Отправляет сообщение в backend и обрабатывает ответ:

```python
process_user_message("Как подключиться к VPN?", 123456)
```

**Логика:**
1. Определяет язык текста
2. Отправляет в `POST /api/ingest`
3. Получает статус (auto-resolve или new ticket)
4. Отправляет соответствующий ответ пользователю

### `get_updates(offset)`

Получает новые сообщения через Long Polling:

```python
updates = get_updates(offset=0)
for update in updates["result"]:
    # Обработка обновления
```

### `main()`

Основной цикл бота:

1. Валидация конфига (TELEGRAM_TOKEN)
2. Вечный цикл с Long Polling
3. Обработка каждого сообщения
4. Обработка ошибок и переподключение

## 🔧 Настройка

### Изменить timeout Long Polling

```python
# telegram_bot.py, функция get_updates()
params = {
    "offset": offset,
    "timeout": 100,  # ← Измените здесь (в секундах)
}
```

### Изменить адрес backend

В `.env`:

```env
BACKEND_URL=http://192.168.1.100:8000
```

### Добавить логирование в файл

Добавьте в `telegram_bot.py` после `logging.basicConfig()`:

```python
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.StreamHandler(),
        logging.FileHandler('telegram_bot.log')  # Логи в файл
    ]
)
```

## 🐛 Отладка

### Проверить токен Telegram

```bash
python -c "import requests; print(requests.get('https://api.telegram.org/bot8283540866:AAES_K_VXOOEWh7vOK6JpTD4adnbs6wyMVM/getMe').json())"
```

### Проверить backend

```bash
curl http://localhost:8000/api/ingest -X POST -H "Content-Type: application/json" -d '{"text":"test", "language":"ru"}'
```

### Просмотр логов

```bash
# Linux/Mac
tail -f telegram_bot.log

# Windows
type telegram_bot.log
```

## 🚨 Обработка ошибок

Бот обрабатывает следующие ошибки без падения:

| Ошибка | Действие |
|--------|----------|
| Backend недоступен | Отправить "Ошибка подключения" |
| Неверный JSON | Отправить "Ошибка обработки" |
| Timeout API | Повторить через 5 сек |
| KeyboardInterrupt (Ctrl+C) | Корректное завершение |

## 📚 Будущие улучшения

- [ ] **Сохранение истории чатов** в БД (для контекста)
- [ ] **Кэширование FAQ** для быстрого поиска
- [ ] **Поддержка файлов** (скриншоты проблем)
- [ ] **Диалоговое меню** (кнопки вместо текста)
- [ ] **Webhook вместо Long Polling** (для production)
- [ ] **Rate limiting** для защиты от спама
- [ ] **Поддержка group chats** и channels
- [ ] **Analytics** — статистика по обращениям

## 📞 Поддержка

Если бот не работает:

1. Проверьте TELEGRAM_TOKEN в `.env`
2. Убедитесь, что backend работает на `http://localhost:8000`
3. Проверьте интернет-соединение
4. Посмотрите логи для ошибок
5. Перезапустите бот

---

**Создано для AI HelpDesk OneWindow** | December 2024
